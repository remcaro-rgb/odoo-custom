"""Hello agent — a trivial end-to-end smoke agent.

Proves the runtime works: reads a port, calls the LLM, files a comment.
Not a "real" agent; used during Phase 6 to validate adapter wiring before
the production agents are built.
"""
