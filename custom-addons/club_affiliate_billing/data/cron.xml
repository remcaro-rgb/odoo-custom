<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="cron_monthly_affiliate_billing" model="ir.cron">
            <field name="name">Club: Monthly Affiliate Billing (Cargo a Socio)</field>
            <field name="model_id" ref="model_club_affiliate_billing_run"/>
            <field name="state">code</field>
            <field name="code">model._cron_monthly_billing()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
